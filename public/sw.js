// Service Worker for FundiGuard PWA
// Auto-generated by next-pwa, but can be customized here

self.addEventListener("install", (event) => {
  console.log("FundiGuard Service Worker installing...");
  self.skipWaiting();
});

self.addEventListener("activate", (event) => {
  console.log("FundiGuard Service Worker activated");
  event.waitUntil(clients.claim());
});

// Handle offline requests (optional custom logic)
self.addEventListener("fetch", (event) => {
  // next-pwa will handle most of this automatically
  // You can add custom offline handling here if needed
});
